/*
This task requires you to use the code segment below to implement linear search

function linearSearch(arr,k)
    n = arr.length
    for i = 0 to n
    if arr[i] == k
    return i;
    else
    return -1
    You can test this using arr = [1,0, 4, 6, 12,14] and k = 4
*/